<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NoteAppKhaled" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/primaryColor</item>
    </style>

    <style name="ThemeOverlay.MyPopupTheme" parent="Theme.AppCompat.DayNight">
        <item name="android:colorBackground">@color/primaryColor</item>
        <item name="android:textColor">@color/bWhite</item>
        <item name="bottomAppBarStyle">@color/bWhite</item>
        <item name="bottomNavigationStyle">@color/bWhite</item>
    </style>

</resources>